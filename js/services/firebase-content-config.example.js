// Copy this file to firebase-content-config.js and fill real values,
// then include it before static-content-provider.js in index.html if needed.
window.FIREBASE_CONTENT_CONFIG = {
  projectId: 'your-project-id',
  apiKey: 'your-web-api-key',
  collection: 'posts'
};
